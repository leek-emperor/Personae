# 显式钉住官方 registry。
#
# 不写的话会继承机器上的全局 ~/.npmrc —— 内网环境常把 registry 指向
# 私有镜像（bnpm / npmmirror 等），那样不同人 clone 后装出来的依赖来源
# 不一致，lockfile 里也可能混进私有域名。CI 上更是必须走官方源。
registry=https://registry.npmjs.org/

# electron-builder 需要扁平的 node_modules 才能正确解析原生依赖
shamefully-hoist=true
